<section>
    <div class="card mb-4">
        <div class="card-header">
            Update Views
        </div>
        <div class="card-body">
            {{#if viewCreate}}

            {{/if}}
            {{#views}}
            {{> views}}
            {{!-- <div>
                <button class="btn btn-sm btn-outline-primary me-2 edit-file">
                    <i class="bi bi-pencil"></i> Edit
                </button>
                <form action="/idp/delete/{{idp.name}}" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-sm btn-outline-danger"
                        onclick="return confirm('Are you sure you want to delete this IDP?');">
                        <i class="bi bi-trash"></i> Delete
                    </button>
                </form>
            </div> --}}
            {{/views}}
        </div>
    </div>
    <div class="card mb-4">
        <div class="card-header">
            Upload View Content
        </div>
        <div class="card-body">
            <div class="container ms-3">
                <form id="uploadContent">
                    <div class="d-flex flex-row align-items-start">
                        <div class="mb-3 me-3">
                            <label for="contentFile" class="form-label">View name</label>
                            <select id="labelSelect{{name}}" class="me-3">
                                {{#views}}
                                <option>{{name}}</option>
                                {{/views}}
                            </select>
                        </div>
                        <div class="mb-3 me-3">
                            <input type="file" class="form-control" id="contentFile" name="contentFile" required>
                        </div>
                        <button type="button" class="common-btn-primary" onclick="uploadContent()">Upload</button>
                    </div>
                </form>
            </div>
            {{!-- <div class="modal fade" id="editFileModal" tabindex="-1" aria-labelledby="editFileModalLabel"
                aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="editFileModalLabel">Update view content</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <input type="hidden" id="editFileId" name="fileId">
                            <form id="editForm" enctype="multipart/form-data">
                                <div class="mb-3">
                                    <label for="editZipFile" class="form-label">Upload New Zip File</label>
                                    <input type="file" class="form-control" id="editZipFile" name="file" accept=".zip"
                                        required>
                                </div>
                                <button type="button" class="btn btn-primary"
                                    onclick="updateOrgContent('{{orgID}}')">Update
                                    File</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div> --}}
        </div>
</section>